
# 01 프로그래밍

## 1.1 프로그래밍이란

- 프로그래밍은 커뮤니케이션
	- 컴퓨터에게 정확하고 상세하게 요구사항을 설명하는 작업
- 컴퓨팅 사고가 중요함

## 1.2 프로그래밍 언어

- 기계어는 사람이 이해 하기 힘듬
- 약속된 구문 (문법)을 구성된 프로그래밍 언어를 작성
	- 구문에 따라 기계어로 변환을 거쳐서 명령
	- 번역기 = 컴파일러 혹은 인터프리터

## 1.3 구문과 의미

- 문법적으로 잘못되지 않더라도 괜찮지만
- 문제를 해결할 수는 없음
- 그렇게 때문에 본질적으로는 옳지 않은 행위임

- 결국 프로그래밍은 요구 사항의 집합을 분석해서 적절한 자료구조와 함수의 집합으로 변환한 후, 그 흐름을 제어하는 것
  
# 02 자바스크립트란 ?

## 2.1 자바스크립트의 탄생

- 넷스케이프에서 만든 브라우저용 경량 프로그래밍 언어
- 모카 =&gt; 라이브스크립트 =&gt; 자바스크립트

## 2.2 자바스크립트의 표준화

- 마이크로 소프트, 익스플로러에 들어가는 자바스크립트의 파생버전인 JScript 개발
- 자바스크립트와 적당히 호환됨
- 웹페이지가 어느 브라우저를 기준으로 하는 지에 따라 정상적으로 동작하지 않은 **크로스 브라우징 이슈**가 발생
- 비영리 표준 단체인 ECMA에 표준 지정을 의뢰
- **ECMA 스크립트의 탄생**

## 2.3 자바스크립트 성장의 역사

### 2.3.1 AJAX

- 서버와 브라우저의 비동기 방식의 데이터 교환을 지원
- 비동기적으로 변하는 부분만 AJAX로 받아오고
- 변화한 부분만 렌더링함.

###  2.3.2 jQuery

- 편리한 DOM 조작을 지원
 
### 2.3.3 V8 자바스크립트 엔진

- 구글이 제작한 성능 좋은 브라우저 엔진
- 많은 코드들이 서버에서 클라이언트로 이동
 
### 2.3.4 Node.js

- V8 엔진으로 구성된 자바스크립트 런타임 환경
- 비동기 I/O
- 단일 스레드 이벤트 루프를 기반으로 동작
	- 처리 성능이 좋음
	
### 2.3.5 SPA 프레임워크

- 웹에 높은 성능과 경험을 요구
- 개발 방법론과 절차가 복잡해짐
- CBD (componenet based developed) 방법론을 기반으로 함

## 2.4 자바스크립트와 ECMAScript

- 자바스크립트의 표준 사양인 ECMA-262

## 2.5 자바스크립의 특징

- 웹 브라우저에서 동작하는 유일한 프로그래밍 언어
- 인터프리터 언어
- 멀티 페러다임 프로그래밍 언어
	- 명령형
	- 함수형
	- 프로토타입 기반 객체 지향 프로그램밍을 지원
## 2.6 ES6 브라우저 지원 현황


# 03 자바스크립트 개발 환경과 실행방법


# 04 변수

## 4.1 변수란 무언인가? 왜 필요한가 ?

- 컴퓨터는 저장공간과 연산 공간이 분리되어 있음
- 저장공간은 여러개의 단위 집합으로 이루어져 있음
- 메모리에 아무 값이나 저장 할 수 있지만, 그 공간을 식별하기 위한 이름이 필요함

- 변수는 하나의 값을 저장히가 위해 확보한 메모리 공간 자체 또는 그 메모리 공간을 식별하기 위에 붙이는 이름을 말한다.

- 할당: 변수에 값을 저장하는 것
- 참조: 변수에 저장된 값을 읽어 들이는 것

## 4.2 식별자, identifier

- 변수 이름을 식별자라고도 한다.
- 식별자는 어떤 값을 구별해서 식별할 수 있는 고유한 이름을 말한다.

- 식별자는 값이 아니라 메모리 주소를 기억하고 있음
	- 그리고 해당 메모리가 값을 가지고 있음
	
- 선언을 통해 자바스크립트에 식별자의 존재를 알린다.

## 4.3 변수 선언

- 변수 선언
	- 변수를 생성하는 것을 말한다.

- 자세히
	- + 메모리 공간을 확보
	- + 변수 이름과 확보된 메모리 공간의 주소를 연결
	- = 값을 저장 할 수 있게 준비
	
- 선언
	- 변수를 사용하려면 반드시 선언이 필요
	- 키워드
		- `var`, `let`, `const`

- 문법
```js
선언_키워드 변수명  = 초기값
```

- 이러한 선언은 엔진의 두 가지 절차에 의해 진행된다.

- **선언 단계**
	- 변수의 이름을 등록
	- ? 변수의 이름은 어디에 등록 될까?
		- 실행 컨텍스트에 등록됨
		- 변수의 이름과 변수의 값은 키 벨류의 형식으로 객체로 등록되어 관리됨
	- ? 실행 컨텍스트, execution context
		- 자바스크립트 엔진이 소스코드를 평가하고 실행하기 위해 필요한 환경을 제공
		- 코드의 실행 결과를 실제로 관리하는 영역
		- 자바스크립트 엔진은 실행 컨텍스트를 통해 식별자와 스코프를 관리함

- **초기화**
	- 초기화란 변수가 선언된 이후 최초로 값을 할당하는 것을 의미.
	- 메모리 공간을 확보하기 위해 실행
	- 암묵적으로 undefined 라는 값이 활당
	
- 초기화를 거치지 않으면 메모리 공간에 이전에 사용한 값이 남아 있어서 오류를 발생 시킬 수 있음

## 4.4 변수 선언의 실행 시점과 변수 호이스팅

- 변수 참조 이후에 변수를 선언하더라도 문제가 발생하지 않음
- 변수 선언은 소스 코드가 한 줄씩 읽히고, 바이트 코드로 변환되어 실행되는 시점인 **런타임** 보다 앞서 실행되기 때문

- 자바스크립트 엔진은 런타임에 앞서, 소스코드의 평가과정을 거치며 실행하기 위한 준비를 한다.
- 평가 과정에서 모든 선언문(변수, 함수 등)을 찾아내 먼저 실행한다.
- 그리고 평가 과정 이후에 모든 코드를 한줄 씩 순차적으로 실행한다.

- 변수 선언문이 코드의 선두로 끌어 올려진 것처럼 작동하는 자바스크립트 고유의 특징을 **변수 호이스팅**이라 한다.

## 4.5 값의 할당

- 값의 할당은 `=` 에 의해 이루어진다.
	- 우변의 값을 좌변에 할당

> [!warning] 주의!
> 변수 선언과 값의 할당의 실행 시점이 다르다.
>- 선언은 런타임 이전에 시행되지만. 할당은 런타임에 일어난다.

## 4.6 값의 재할당

- 값의 재할당은 변수에 새로운 값을 다시 할당하는 것이다.
- 값을 재할당 할 수 없는 것은 상수라고 한다.
	- const 키워드를 사용하여 상수를 표현한다.
	
- 재할당의 과정
	- 재할당은 값이 저장된 주소의 값을 변경하는 것이 아니라,
	- 새로운 메모리 공간을 확보하고,
	- 식별자에 연결된 주소를 다시 매칭하고
	- 새로운 메모리 공간에 재할당 하려는 값을 저장하는 방식으로 동작한다.

- 사용되지 않는 메모리 공간은 가비지 콜렉터가 정리한다.
	- 단, 정확한 시점이 정해져 있는 것은 아니므로, 저장소가 비어있다는 확신을 할 수 없는 것이다.
	
## 4.7 식별자 네이밍 규칙
- 예약어는 사용 불가
- 언더 스코어를 사용할 수는 있지만 언더스코어로 시작하면 안된다.


# 05 표현식과 문

## 5.1 값

- 값
	- 표현식이 평가되어 생성된 결과
- 평가
	- 식을 해석해서 값을 생성하거나 참조하는 것
- 변수
	- 하나의 값을 저장하기 위해 확보한 메모리 공간
	- (또는) 그 메모리 공간을 식별하기 위해 붙인 으름 

## 5.2 리터럴

- 리터럴
	- 사람이 이해 할 수 있는 문자
	- (또는) 약손된 기호르 상요해 값을 생성하는 표기법
	- 자바스크립트 엔진은 런타임에 리터럴을 평가해 값을 생성
	- 즉, 리터럴은 값을 생성하기 위해 미리 약속한 표기법

## 5.3 표현식

- 표현식
	- 값으로 평가 될 수 있는 문
	- 표현식이 평가되면 새로은 값을 생성하거나 기존 값을 참조

- 값으로 평가될 수 있는 문은 모두 표현식
	- 리터럴 표현식
	- 식별자 표현식
	- 연산자 표현식
	- 함수 메서드 호출 등

## 5.4 문

- 문
	- 프로그램을 구성하는 기본 단위, 최소 실행 단위
	- 이것의 집합이 프로그램
	- 문을 작성하는 것이 프로그래밍

- 문은 토큰으로 구성됨

- 토큰
	- 문법적 의미를 가지며 문법적으로 더 이상 나눌 수 없는 코드의 기본 요소
	- 키워드, 식별자, 연산자, 리터럴 세미콜론, 마침표 등

- 문 = 명령문 = 컴퓨터에 내리는 명령

- 할당문, 조건문, 반복문 등


## 5.5 세미콜론과 세미콜론 자동 삽입 기능
- 세미콜론 = 문의 종료를 의미
	- 문을 끝낼때는 세미콜론이 필요함
	- 단, 중괄호로 묶은 코드 블록에서는 아님
		- `if`, `for`, 함수 등의 코드 블록은 자체 종결성을 가짐

- 생략 가능함, 왜?
	- 자바스크립트 엔진이 적당히 붙임
	- 세미콜론 자동 삽입 기능(ASI)가 암묵적으로 수행됨

- 하지만 맹신은 X
	- 동작이 예상을 벗어나는 경우가 존재
	- 누군가는 다 붙여야 한다고 주장하기도

## 5.6 표현식인 문과 표현식이 아닌 문
- 구분하는 쉬운 방법은 변수 할당해보기
	- 표현식인 문은 값으로 평가 = 변수에 할당됨
	- 표현식이 아니다 = 값으로 평가 불가 = 변수 할당 불가
-
