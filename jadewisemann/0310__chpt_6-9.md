
# 06 데이터 타입
- 데이터 타입은 값의 종류를 말한다
- 자바크립트의 모든 값은 데이터 타입을 갖는다.
- 7개가 있는데 6개의 원시 타입과 1개의 객체 타입

## 6.1 숫자 타입
- [[배정밀도 64비트 부동 소수점]] 형식
	- 모든 수를 실수로 처리
	- 정수만을 표현하는 데이터 타입(interger)는 존재하지 않음
- 특수 값도 표현 가능
	- `Infinity`: 양의 무한대
	- `-Infinity`: 음의 무한대
	- `NaN`: 산술 연산 불가, Not a Number
	
- js는 대소 문자 구분, NaN != NAN, Nan, nan

## 6.2 문자열 타입
- 텍스트 데이터 표현
- 0개 이상의 [[16비트 유니코드 문자]]의 집합
- 작은 따옴표('') 또는 큰 따옴표("") 또는 백틱으로 텍스트를 감싸 사용
	- 키워드나 식별자와 같은 토큰과 구분하기 위함

## 6.3 템플릿 리터럴
- ES6부터 추가된 새로운 문자열 표기법
- 백틱을 사용

### 6.3.1 멀티라인 문자열
- 줄바꿈과 모든 공백이 허용됨
- 일반 문자열은 이스케이스 스퀀스 사용해야 함

### 6.3.2 표현식 삽입
- `${}`으로 표현식을 감싸서 중간에 삽입이 가능
- 문자열이 아니더라도 문자열로 형 변환되어 삽입
 
## 6.4 불리언 타입
- 논리적 참, 거짓을 나타내는 `true`, `false` 뿐

## 6.5 `undefined` 타입
- `undefined`가 유일
- `var`키워드로 선언한 변수는 암묵적으로 `undefined`로 초기화 됨
- 변수 선언을 확보된 메모리 공간을 빈상태가 아니라 `undefined`가 할당되어 있음
- 의도적이 값이 아니라 자바스크립트 엔진이 변수를 초괴화할때 사용하는 값

## 6.6 `null` 타입
- `null`이 유일
- 대소문자를 구분, `null` != `Null`, `NULL` 
- 의도적으로 변수에 값이 없다는 것을 명시할 때 사용(의도적 부재)

## 6.7 심벌 타입
- 변경 불가한 원시 타입
- 다른 값과 중복되지 않는 유일한 값
	- 객체의 유일한 프로퍼티 키를 만들기 위해 사용
- 리터럴 없음
	- `Symbol` 함수 호출해 생성
	- 이는 외부에 노출 되지 않음
	- 다른 값과 중복되지 않는 유일무의한 값

## 6.8 객체 타입
- 원시 타입 외의 모든것
- **자바스크립트를 이루고 있는 거의 모든 것이 객체**

## 6.9 데이터 타입의 필요성

### 6.9.1 데이터 타입에 의한 메모리 공간 확보와 참조
- 값을 저장하려면 메모리 공간의 크기를 알아야 함
- 타입에 따라 크기가 다르므로 메모리 공간을 효율적으로 사용 가능

### 6.9.2 데이터 타입에 의한 값의 해석
- 메모리에는 이진수 숫자만 있음
- 동일한 이진수 패턴이라도 타입에 따라 다르게 해석해서 정확한 값으로 사용하기 위함

## 6.10 동적 타이핑

### 6.10.1 동적 타입 언어와 정적 타입 언어
- 정적 타입 언어
	- 변수 선언 시 타입을 사전에 선언해야함
	- 이를 "**명시적 타입 선언**"이라 함
	- 타입의 변경이 불가
	- 타입에 맞는 값만 할당 가능
	- 컴파일 시점에 타입 체크를 수행
		- 통과 못하면 실행을 막음

- js는?
	- 타입 선언 하지 않음
	- `var`, `let`, `const` 키워드로 선언
	- 어떤 타입도 할당 가능

- js에서는 타입이 선언에 의해서 결정이 되지 않음
- 할당에 의해서 타입이 결정(추론)됨
	- 재할당에 의해서 언제나 동적으로 변경이 가능

- 이런 것을 "**동적 타입**"이라고 함

### 6.10.2 동적 타입 언어와 변수
- 트레이드 오프가 있음
- 유연한 대신 신뢰도가 낮음
- 오히려 유연성을 제한하기 위해서 코드가 더 필요하기도

# 07 연산자 (operator)

## 7.1 산술 연산자 (arithmetic ..)

### 7.1.1 이항 산술 연산자 (binary ...)
- 2개의 피연산자를 산술
- `+`(더하기), `-`(빼기), `*`(곱하기), `/`(나누기), `%`(나머지) 

### 7.1.2 단항 산술 연산자 (unray ...)
- 한개의 피연산자를 산술 연산
- `++`(증가), `--`(감소), `+`(효과 없음), `-`(반전)
 
- 단, `++`, `--`는 값을 변경하는 부수 효과 있음
	- 피연산자의 값을 변경하는 암묵적 할당 있음

- `++`, `--`는 위치에 의막 있음
	- 피연산자 앞에 있다 => 값을 증가/감소 시키고 다른 연산 시행
	- 피연산자 뒤에 있다 => 다른 연산을 하고 값을 증가/감소 시킴

- `+`는 효과가 없다
	- 단, 숫자가 아닌 값의 앞에 위치 시키면 숫자로 변환하여 반환
	- 원본을 변형하지 않음 => 부수 효과 없음

- `-`는 반전함
	- 원본 값을 수정하지 않고 반전된 값을 반환 => 부수 효과 없음

### 7.1.3 문자열 연결 연산자
- **`+`연산자는 피연산자 중 하나 이상이 문자열인 경우 문자열 연산자로 동작**
	- 그외에는 산술 연산자

## 7.2 할당 연산자 (assignment ...)
- 우항에 있는 피연산자의 평가 결과는 좌항에 있는 변수에 할당
- 할당 연산자는 변수의 값을 할당 => 당연히 변수의 값이 변하는 부수 효과

- `=`, `+=`, `-=`, `*=`, `/=`, `%=`

- **할당문은 값으로 평가되는 표현식인 문으로서 할당된 값으로 평가됨**

```js
// ---
var x;

console.log(x = 10); // 10, 10으로 할당되고 10으로 평가됨

// ---
var a, b, c;

a = b = c = 0;
// c = 0 은 0으로 평가됨
// b = 0은 0으로 평가됨


console.log(a, b, c); // 0 0 0  
```

## 7.3 비교 연산자 (comparison ...)
- 좌항과 우항의 피연산자 비교 => 결과를 불리언으로 반환

### 7.3.1 동등/일치 비교 연산자
- 둘은 좌항 우항 피연산자의 일치를 비교하여 불리언 반환
	- 단 엄격도가 다름
- 동등 비교(loose equality) 연산자
	- `==` : 좌항 우항의 값이 값음 (동등 비교)
	- `!=` : 좌항 우항의 값이 다름 (부동등 비교)
- 일치 비교(strict equality) 연산자
	- `===` : 좌항 우항의 값과 타입이 같음 (일치 비교)
	- `!==` : 좌항 우항의 값과 타입이 다름 (불일치 비교)

- 또한 동등비교 연산자는
	- **비교할때 암묵적 타입 변환을 통해 타입을 일치 시킨후 비교**
	- 일치 비교는 하지 않는다 => 예측 가능함

### 7.3.2 대소 관계 비교 연산자
- 크기를 비교하여 불리언 값을 반환
- `>`, `<`, `>=`, `<=`

## 7.4 삼항 조건 연산자 (termary ...)
- 조건식 ? 조건식이 true 일때 반환할 값 : false 일때 값

- 삼항 조건 연산자 표현식은 값으로 평가할 수 있는 표현식인 문
	- 값처럼 다른 표현식에 할당 가능

## 7.5 논리 연산자 (logical ...)
- 우항과 좌항의 피연산자를 논리 연산
	- 단 부정은 우항의 피연산자를 논리 연산

- `||`(논리합, or), `&&`(논리곱, and), `!`(부정, not)

- 논리 부정 연산자는 언제나 불리언 값을 반환
	- 피연산자가 불리언이 아니어도 됨
		- 아니면 암묵적 타입 변환후 부정

- 논리합, 논리곱의 평가 결과는 불리언이 아닐 수도 있음
	- 언제나 2개의 피연산자 중 어느 한쪽으로 평가됨

## 7.6 쉼표 연산자
- 왼쪽 피연산자부터 차례대로 피연산자를 평가
- 마지막 피연산자의 평가가 끝나면 마지막 피연산자의 결과를 반환

```js
var x, y, z;

x = 1, y = 2, z = 3; // 3
```

## 7.7 그룹 연산자
- 소괄호`()`로 피연산자를 감싼다.
- 자신의 피연산자인 표현식을 가장 먼저 평가
	- 우선순위 조절 가능

## 7.8 `typeof` 연산자
- 피연산자의 데이터 타입을 문자열로 반환
- `null`을 반환하지 않음
	- `null`은 `object`를 반환
	- 따라서 일치 연산자를 `null`과 함께 피연산 해서 확인하자
- 함수는 `function`을 반환 (`object`임에도)

## 7.9 지수 연산자
- 좌항 피연산자를 밑으로, 우항 피연산자를 지수로
```js
2 ** 2 // 4
2 ** 3 // 8
2 ** 0 // 1
```

- 음수를 밑으로 하려면 괄호로 묶어야 함
```js
-5 ** 2 // SyntaxError

(-5) ** 2 // 25
```

## 7.10 그 외의 연산자
- 뒤에서 보자

## 7.11 연산자의 부수 효과
- 다른 코드에 영향을 줄 수 있다
- 할당 연산자(`=`), 증가/감소 연산자(`++/--`), `delete` 연산자

## 7.12 연산자 우선순위
- 여러개의 연산자로 이뤄진 문이 실행 될때 실행되는 순서
	- 우선 순위 높을 수록 먼저

## 7.13 연산자 결합 순서
- 연산자의 좌항 우항 중 어느쪽 부터 평가를 수행할지 나타내는 순서


# 08 제어문

## 8.1 블록문
- 0개 이상의 문을 중괄호로 묶은 것
- 문의 끝에는 세미콜론을 붙인다
	- 중괄호는 자체종결성을 가지고 있어서 붙이지 않음

## 8.2 조건문
### 8.2.1 `if ... else` 문

```js
if (조건식) {
  // ...
}
else if (조건식 2) {
	// ...
} else {
	// ...
}
```

- 조건식은 불리언으로 평가되어야 한다
	- 아닌값으로 평가됨 => 암묵적으로 형변환

- `else if`는 여러번 가능, `else`는 한번

### 8.2.2 `switch` 문
```js
switch (표현식) {
	case 표현식1:
		//표현식 == 표현식1이면 실행;
		break;
	case 표현식2:
		//표현식 == 표현식2이면 실행;
		break:
	default:
		// 일치하는 케이스 없을때 시행
}
```

- `break`가 없으면 조건문 이후 탈출 하지 않는다
	- 조건의 관계없이 무조건 `default`가 시행된다
	- 이를 fall throught 라고 한다
- 따라서 `break`는 의도대로 동작하기 위해 반드시 필요하다. 
 
## 8.3 반복문
- 조건문의 평가 결과가 참인 경우 코드블록을 실행
- 다시 평가 후 참이면 ....
- 조건문이 거짓일때 까지 반복

### 8.3.1 `for`문
- 조건식이 거짓으로 평가될 때 까지 코드 블록을 반복

```js
for (변수 선언 or 할당문; 조건식; 증감식) {
	// 조건식이 참인 경우 실행되는 식
}
```

### 8.3.2 `while` 문
- 조건식이 평가 결과가 참이면 코드 블록을 반복 실행

```js
while (조건식) {
	// 조건식이 참일 경우 실행되는 식
}
```

- 조건식의 평가 결과가 언제나 참이면 무한 루프
	- `break`로 코드 블록 탈출 필요
	
```js
while (true) {
	// 조건식이 참일 경우 실행되는 식
}
```

### 8.3.3 `do ... while` 문
- 코드 블록을 *먼저 실행하고* 조건식을 평가.
	- 따라서 무조건 한 번 이상 실행

```js
do {
	// 먼저 실행됨
} while (조건식) {
	// 조건식이 참일 경우 실행되는 식
}
```


## 8.4 `break` 문
- 코드 블록을 탈출한다
	- 엄밀히: 레이블 문, 반복문 또는 `switch` 문의 코드 블록을 탈출
	- 이 외에서는 `SyntaxError`

- 레이블 문
	- 식별자가 붙은 문
	
```js
foo: {
	break foo;
} // foo 식별자가 붙은 문
```


## 8.5 `continue` 문
- 코드 블록을 즉시 중단 후 증감식을 이동

# 09 타입 변환과 단축 평가
## 9.1 타입 변환이란?
- 명시적 타입 변환(explict coercion)
	- = 타입 캐스팅(type casting)
	- 개발자가 의도적으로 값의 타입을 변환하는 것

- 암묵적 탕비 변환(implict coercion)
	- = 타입 강제 변환(type coercion)
	- 개발자의 이도와 상관 없이 자바스크립트 엔진이 변환하는 것

- 원시값 직전 변경은 불가함
	- 형변환은 기존 값을 이용해서 새로운 값을 만드는 것

## 9.2 암묵적 타입 변환
### 9.2.1 문자열 타입으로 변환
- `+`는 두 피연산자 중 하나 이상이 문자열이면 => 문자열 피연산자
	- 따라서 다른 하나의 타입이 뭐든 문맥적으로 문자열로 판단

### 9.2.2 숫자 타입으로 변환
- `-`, `*`, `/` 등의 산술 연산자의 역할 => 연산하는 것
	- 문맥적으로 숫자로 사용한다고 판단
- 숫자 변환을 시도
- 안되면 `NaN`으로 변환

### 9.2.3 불리언 타입으로 변환
- 조건식 등은 논리적으로 참 거짓을 평가하고 싶어함
	- 문맥적으로 논리값으로 사용한다고 판단

- `Truthy`값
	- 참으로 여겨지는 값
	- 또는 참으로 형변환 되는 값
	- Falsy 왜 전부

- `Falsy` 값
	- 거짓으로 여겨지는 값
	- 또는 거짓을 형변환 되는 값
	- `false`, `undefined`, `null`, `0`, `-0`, `Nan`, `''`


## 9.3 명시적 타입 변환

### 9.3.1 문자열 타입으로 변환
- `String` 생성자를 `new` 연산자 없이 호출
- `Object.prototype.toString` 매서드 사용
- 문자열 연결 연산자를 사용
	- `+`에 다른 피연산자가 문자열인 경우

### 9.3.2 숫자 타입으로 변환
- `Number` 생성자를 `new` 연산자 없이 호출
- `parseInt`, `parseFloat` 함수를 사용(단, 문자열만)
- `+` 단항 산술 연산자 사용
- `*` 산술 연산자 사용

### 9.3.3 불리언 타입으로 변환
- `Boolean` 생성자 함수를 `new` 연산자 없이 호출
- `!` 부정 논리 연산자를 두번 사용 하는 방법
	- 첫번째 `!`에서 형변환 후 부정, 두번째 `!`에서 원래 논리로 복구

## 9.4 단축 평가
### 9.4.1 논리 연산자를 사용한 단축 평가
- 논리곱 연산자
	- 두 개의 피연산자가 모두 true 일때 true
	- 좌항에서 우항으로 평가 진행
	- 우항이 true면 논리곱 연산자의 평가는 우항에 의존
		- 만약 우항이 true면 우항을 바로 리턴함

- 논리합 연산자
	- 좌항이 truthy하면 이미 true
		- 좌항 리턴
	- falsy하면 우항 평가 => 우항이 truthy하면
		- 우항리턴

- 즉 평가를 결정짓는 값을 반환한다
	- 타입 변환하지 않고 반환
	- 이를 단축 평가라 함
		- 평가 도중 평가결과가 확정되면 나머지는 생략

- *객체를 참조할 때* => 논리곱 연산으로 객체가 truthy 할때만 참조
- *함수 매개변수의 기본값을 정할 때* => 논리합 연산으로 falsy 할때 할당

### 9.4.2 옵셔널 체이닝 연산자
- `?.`
	- 좌항이 `null` 또는 `undefined` => `undefined` 반환
	- 아니면 우항을 참조
- *객체 참조* 시 유용하다 

### 9.4.3 `null` 병합 연산자
- `??`
	- 좌항이 `null` 또는 `undefined` => 우항 반환
	- 아니면 좌항 반환
- *함수 매개변수의 기본값을 정할때* 유용